# 2026-03-04 — Memory Flush

## Model & Orchestration Changes
- Switched orchestrator to `openai/gpt-5.1` for main OpenClaw/Discord agent; specialist models unchanged:
  - Architect & Lorekeeper → `moonshot/kimi-k2.5`
  - Steward → `ollama/minimax-m2.5:cloud`
  - Systems → `ollama/glm-5:cloud`
- Confirmed Kimi’s vision + swarm capabilities make it the right fit for Architect and Lorekeeper; GLM-5 reserved for Systems and (future) cloud security focus.

## Security & Cloud Migration
- Reviewed a Claude-drafted security/system prompt for future cloud deployment; rejected parts that are too rigid for local (e.g., blanket bans on shell config edits) but kept the concepts.
- Designed and committed `Projects/cloud-security-requirements.md` in Basilica:
  - Zero-trust model for future VPS (likely Oracle Free Tier ARM).
  - Trust hierarchy (system prompt/user > tools > external data).
  - Shell safety (intent, blast radius, exfiltration checks; safe-word for destructive ops).
  - Network hardening (Tailscale, default-deny), secret management, container hardening, and logging/alerting.
- Decision: cloud migration is a later project; security design is locked in now so Systems can implement when we choose to move.

## Task Surfacing / ADHD Support
- Clarified need: daily surfacing of **important but non-urgent / undated** tasks (job hunt, paperwork), plus calendar-tied deadlines and overdue items.
- Systems designed and we committed `Projects/task-surfacing-spec.md`:
  - Daily 09:00 check-in, separate from existing 08:00 digest.
  - Task sources: CalDAV calendars, structured `01_Daily/todo.md`, and keyword-based detection (e.g., "apply", "submit", "deadline").
  - Priority tiers: CRITICAL/HIGH/MEDIUM/LOW based on deadlines, overdue status, and age of undated tasks.
  - Overdue handling: extend deadline / mark complete / cancel.
  - Markdown daily checklist with commands Steward can process (e.g., complete/extend/remind/snooze).
- Decision: spec is “locked in”; Steward will implement later in phases (foundation → pipeline → Discord integration).

## Agent Capabilities & Swarms
- Documented Kimi swarm usage explicitly in `AGENTS.md` for both Architect and Lorekeeper (after fixing a partial update caused by GLM API rate limiting):
  - Architect: parallel sub-agents for design files, visual QA, multi-file/codebase comparisons.
  - Lorekeeper: parallel sub-agents for multi-source research and claim verification.
- Confirmed architect/lorekeeper division: Kimi handles visual+research-heavy tasks; GLM-5 handles infra/security; MiniMax handles clerk work.

## Philosophy & Meta
- Captured a key user principle in both `USER.md` and `08_System/system-context.md`:
  - **Intellectual friction is necessary for growth** — automate busywork, but encourage user-led exploration for cognitively rich tasks.
- Updated working philosophy section so agents know to provide scaffolding rather than instant answers when a task has clear growth value.
- Agreed on more **atomic responses** to preserve credits: one idea per reply, no preamble/summary, conclusion first.

## Open Loops
- Task surfacing automation: spec done, implementation pending (Steward + automation scripts/Lobster).
- Cloud migration: security requirements designed, but no infra yet; to be picked up when we explicitly choose to move off local-only.
- Neon Life refactor and calendar-aware digest enhancements remain future work, unchanged by today’s decisions.
